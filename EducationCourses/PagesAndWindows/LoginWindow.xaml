<Window x:Class="EducationCourses.PagesAndWindows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EducationCourses.PagesAndWindows"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid Background="#F0F8FF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E8B57" Padding="15">
            <TextBlock Text="СИСТЕМА УПРАВЛЕНИЯ КУРСАМИ" 
                      Foreground="White" FontSize="16" FontWeight="Bold" 
                      HorizontalAlignment="Center"/>
        </Border>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" Margin="30" VerticalAlignment="Center">

            <TextBlock Text="Вход в систему" FontSize="20" FontWeight="Bold" 
                      Foreground="#2E8B57"/>

            <Border Background="White" Padding="15" CornerRadius="5" BorderBrush="#DDD" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Логин:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtLogin" Height="30" FontSize="14" Padding="5"/>

                    <TextBlock Text="Пароль:" Margin="0,15,0,5" FontWeight="SemiBold"/>
                    <PasswordBox x:Name="txtPassword" Height="30" FontSize="14" Padding="5"/>

                    <Button x:Name="btnLogin" Content="ВОЙТИ" Click="btnLogin_Click" 
                           Height="35" Margin="0,25,0,0" Background="#2E8B57" 
                           Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0"/>
                </StackPanel>
            </Border>

            <Button x:Name="btnRegister" Content="РЕГИСТРАЦИЯ" 
                   Click="btnRegister_Click" Height="30" Margin="0,15,0,0" 
                   Background="Transparent" Foreground="#2E8B57" 
                   FontWeight="SemiBold" BorderThickness="0"/>

            <TextBlock x:Name="txtMessage" Foreground="Red" Margin="0,10,0,0" 
                      TextWrapping="Wrap" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10">
            <TextBlock Text="Дополнительное образование © 2024" 
                      Foreground="#666" FontSize="11" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
